import { Router } from 'express';
const router = Router();

// router.get('/',async (req, res)=>{

//     const data = await query("select * from User_Schema.UserTable");

//     res.send(data)
// })

export default router;
